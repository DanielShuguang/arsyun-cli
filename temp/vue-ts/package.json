{
  "name": "{{ appName }}",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "vite preview",
    "lint": "eslint ./src/"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}